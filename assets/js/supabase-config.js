// Supabase 配置
// 未配置时（保持空字符串）站点会自动降级为 localStorage 存储，功能仍可用
// 配置步骤见 README.md → "接入 Supabase"
window.SUPABASE_CONFIG = {
  url: "",       // 例如 "https://xxxxxxxxxxxx.supabase.co"
  anonKey: "",   // 例如 "eyJhbGciOi..."（Settings → API → anon public）
};
